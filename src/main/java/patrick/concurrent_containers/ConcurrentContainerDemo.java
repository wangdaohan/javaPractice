package patrick.concurrent_containers;

/**
 * 并发容器类：ConcurrentHashMap/CopyonWriteList/ConcurrentLinkedQueue
 * 同步容器类：即不支持多线程访问的容器类（线程不安全,或在高并发情况下的性能太差） ->  Vector(线程安全，但不是高并发）、 ArrayList（线程不安全，且不支持高并发）
 */
public class ConcurrentContainerDemo {
    public static void main(String[] args) {

    }
}
